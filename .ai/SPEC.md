# SCSS Skeuomorphism Overhaul

Goal: Replace Bootstrap styles with custom skeuomorphic SCSS.

Constraints:

- Remove Bootstrap entirely
- Mobile behavior unchanged

Acceptance:

- Shared depth/shadow tokens
- Consistent skeuomorphic language
- create a /admin url that allows me to adjust css parameters and copy code to give back to claude
- admin page should include all the different kinds of html elements we use in the site and show examples like a css library
