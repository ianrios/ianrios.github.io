# Metaballs / ThreeScene overhaul — future spec (Ian, 2026-07-07)

Not scheduled. Home for ThreeScene/metaballs ideas that don't belong in the
portfolio-v2 epic. Pixel-morph moved here from
`.ai/plans/portfolio-v2-concepts.md` section 8 — the V2 epic uses the
papers-on-a-table metaphor for page-to-page transitions instead.

- **Pixel-morph transition** (original portfolio-overhaul.md item 18): on
  entry, pixel-morph from the splash into the main page; when navigating to
  any other route (design system, ThreeScene, imagebox, etc.), continue
  pixel-morphing back toward the metaballs visual before landing. Must be
  configurable in admin settings, not hardcoded on.
- Prior related work: `.ai/completed/metaballs-update.md` (design panel
  reuse for a metaballs controls panel, `/three` route cleanup) — already
  done, listed here only as background for whoever picks this up.
