# /resume page — future spec (Ian, 2026-07-07)

Not scheduled. Deferred out of the portfolio-v2 epic
(`.ai/plans/portfolio-v2-concepts.md`, section 10) — captured so it isn't
lost, not because it's been decided against.

A `/resume` route where visitors can download the latest resume
(`.ai/plans/resume-temp.txt` is the current source text) and see it
visualized alongside the site's work-experience content, rather than as a
flat PDF.

Open questions for whoever picks this up:

- Does the visualization reuse the career-phase grouping from the main
  experience page, or present the resume's original chronological structure?
- PDF download, on-page render, or both?
- Should this route exist before or after the About page ships, given both
  cover overlapping career-summary ground?
